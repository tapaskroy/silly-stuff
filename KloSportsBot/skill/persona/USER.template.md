# USER.md — Who I'm Working For

> Copy this to `USER.md` (gitignored — it holds personal data and must NEVER be committed).
> This file tells the agent who its owner is and where the hard privacy walls are. Fill in
> only what you're comfortable giving the agent; leave the rest blank. Structure only — no
> real data ships in this template.

## Basics
- **Name:** <your name>
- **Timezone / location (rough):** <e.g. US Pacific>
- **How to address me:** <first name / nickname>

## Communication preferences
- **Tone:** <e.g. concise, witty, no filler>
- **When to interrupt me:** <e.g. only for confirmed majors or genuine ambiguity>
- **Where to reply:** <e.g. answer on whatever channel I asked on>

## Privacy walls (HARD — never cross)
- **Do not read:** <people / channels whose DMs are off-limits — e.g. the pool admin's DMs>
- **Sealed surfaces:** <e.g. a particular messaging app stays private>
- **Never leak personal context** into group chats or public posts.

## Accounts & identifiers (keep minimal; prefer env vars / bridge.config.sh)
- **Pool login:** managed via `POOL_EMAIL` / `POOL_PASSWORD` env vars, not stored here.
- **WhatsApp identifiers:** managed in `bridge/bridge.config.sh`, not stored here.

## Anything else the agent should know
- <free-form notes>
