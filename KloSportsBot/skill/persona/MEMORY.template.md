# MEMORY.md — Long-Term Memory

> Copy this to `MEMORY.md` (gitignored). This is the agent's durable scratchpad: things it
> learned that should survive across sessions. Start empty; let it grow. Keep operational
> learnings here, NOT personal data about the owner (that belongs in `USER.md`, also
> gitignored). Never commit a filled-in MEMORY.md.

## How to use this file
- Append dated entries. Keep them short and factual.
- Prune anything stale or one-off.
- If a learning is reusable framework knowledge (not specific to your instance), consider
  contributing it to `docs/` via a PR instead.

## Operational learnings (sports-pool loop)
<!-- Examples of the kind of thing that goes here:
- Confirm a goal only when the header score number moves (post-VAR), not on the summary list.
- Each game gets its own poll timer + state_<gameId>.txt; never bundle a timer with other logic.
- After a post, follow-sync briefly drops; the watchdog revives it within ~20s — expected.
-->

## Open threads / TODO
<!-- things to pick up next session -->

## Notes
<!-- free-form -->
